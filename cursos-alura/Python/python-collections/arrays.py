import numpy as np

import array as arr

numeros = np.array([1, 3.5])
print(numeros)

# Evitamos usar array puro do python, se precisamos usar numeros, é comum usar a numpy !!
# !pip install numpy - para instalar a biblioteca